namespace = fe_test

character_event = {
	id = fe_test.1
	
	hide_window = yes
	is_triggered_only = yes 
	
	immediate = {
		if = {
			limit = {
			religion_group = xxx
			}
			add_trait = depressed
		}
		if = {
			limit = { NOT = {
			religion_group = xxx
			} }
			add_trait = drunkard
		}
	}
	
	option = {
		name = OK
	}
}
character_event = {
	id = fe_test.2
	
	hide_window = yes
	is_triggered_only = yes 
	trigger = {
		can_inherit_major_loptyr_holy_blood_trigger = yes
	}
	
	immediate = {
		if = {
			limit = {
				can_inherit_major_loptyr_holy_blood_trigger = yes
			}
			set_character_flag = can_inherit_major_loptyr_holy_blood
		}
		if = {
			limit = {
				can_inherit_major_loptyr_holy_blood_trigger = no
			}
			set_character_flag = cannot_inherit_major_loptyr_holy_blood
		}
	}
	
	option = {
		name = OK
	}
}	
character_event = {
	id = fe_test.3
		
	is_triggered_only = yes
	
	option = {
		random_demesne_province = {
			add_holding_slot = 1
		}
	}
}
character_event = {
	id = fe_test.4
		
	is_triggered_only = yes
	
	option = {
		any_claim = {
			limit = {
				OR = {
					de_jure_liege_or_above = k_verdane
					de_jure_liege_or_above = k_agustria
				}
			}
			remove_claim = PREV
		}
	}
}	
character_event = {
	id = fe_test.5
		
	is_triggered_only = yes
	
	option = {
		if = {
			limit = {
				#ROOT = { is_vassal_or_below = k_miletos }
				ROOT = { any_realm_character = { has_landed_title = k_miletos } }
			}
			prestige = 100
		}
	}
	option = {
		if = {
			limit = {
				#ROOT = { is_vassal_or_below = d_agusty }
				ROOT = { any_realm_character = { has_landed_title = d_agusty } }
			}
			prestige = 100
		}
	}
	option = {
		if = {
			limit = {
				#ROOT = { is_vassal_or_below = k_verdane }
				ROOT = { any_realm_character = { has_landed_title = k_verdane } }
			}
			prestige = 100
		}
	}
}
character_event = {
	id = fe_test.6
		
	is_triggered_only = yes
	
	option = {
		fix_loptyrian_bishop_capitals_effect = yes
	}
}