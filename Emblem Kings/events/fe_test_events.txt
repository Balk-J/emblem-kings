namespace = fe_test

character_event = {
	id = fe_test.1
	
	hide_window = yes
	is_triggered_only = yes 
	
	immediate = {
		if = {
			limit = {
			religion_group = xxx
			}
			add_trait = depressed
		}
		if = {
			limit = { NOT = {
			religion_group = xxx
			} }
			add_trait = drunkard
		}
	}
	
	option = {
		name = OK
	}
}
character_event = {
	id = fe_test.2
	
	hide_window = yes
	is_triggered_only = yes 
	
	immediate = {
		random_playable_ruler = {
			limit = {western_isles_rebel_target_trigger = yes}
			add_trait = war_dragon
		}
		if = {
			limit = {
				NOT = {
					any_independent_ruler = { western_isles_rebel_target_trigger = yes }
				}
			}
			add_trait = depressed
		}
		if = {
			limit = { 
				k_the_western_isles = {
#					any_previous_holder = {
#						always = yes
#					}
					has_holder = yes
				}
			} 
			add_trait = drunkard
		}
	}
	
	option = {
		name = OK
	}
}	